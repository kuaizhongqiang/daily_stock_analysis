# 改动清单 — 第二刀：砍掉 Bot 渠道 + 通知模块

## 已完成操作

### 1. 删除 `bot/` 整个目录
- 包含 `commands/`（10个命令文件）、`platforms/`（5个平台文件）、`dispatcher.py`、`handler.py`、`models.py`、`__init__.py`
- 涉及的钉钉/飞书/Discord Bot 代码全部移除

### 2. 删除 `notification_sender/` 中所有 sender 文件
删除以下 12 个文件：
- `astrbot_sender.py`
- `custom_webhook_sender.py`
- `discord_sender.py`
- `email_sender.py`
- `feishu_sender.py`
- `gotify_sender.py`
- `ntfy_sender.py`
- `pushover_sender.py`
- `serverchan3_sender.py`
- `slack_sender.py`
- `telegram_sender.py`
- `wechat_sender.py`

### 3. 清理 `src/notification_sender/__init__.py`
- 清空所有 sender 导出，改为空模块

### 4. 重写 `src/notification.py`
- 移除 `BotMessage` import
- 移除所有 12 个 sender 的 import
- 移除多继承的 `NotificationService` 类（继承自所有 sender 基类）
- 移除渠道上下文方法：`_has_context_channel`、`_source_platform`、`_extract_dingtalk_session_webhook`、`_extract_feishu_reply_info`、`_extract_telegram_context_chat_id`
- 移除发送方法：`send_to_context`、`_send_via_source_context`、`_send_feishu_stream_reply`、`_send_feishu_stream_chunked`
- `NotificationChannel` 枚举精简为仅保留 `UNKNOWN`
- `ChannelDetector` 精简
- `send()` 方法改为记录日志的桩方法
- `is_available()` 返回 `False`
- 保留核心报告生成方法：`generate_aggregate_report`、`generate_daily_report`、`generate_dashboard_report`、`generate_brief_report`、`generate_wechat_dashboard`、`generate_wechat_summary`

### 5. 清理 `src/notification_routing.py`
- 移除 `discord`、`slack`、`astrbot` 三个渠道

### 6. 清理 `src/notification_capabilities.py`
- 移除 `discord`、`slack`、`astrbot` 三个 `ChannelProfile`
- 移除 `dingtalk`、`slack` 两个 `RendererPreset`

### 7. 清理 `main.py`
- `start_bot_stream_clients()` 函数体置空（原启动钉钉/飞书 Stream 客户端）

### 8. 清理 `src/core/pipeline.py`
- 移除 `from bot.models import BotMessage` import
- `source_message` 类型标注改为 `Optional[Any]`

### 9. 清理 `src/services/task_service.py`
- 移除 `from bot.models import BotMessage` import
- 两处 `source_message` 类型标注改为 `Optional[Any]`

### 10. 清理 `src/services/system_config_service.py`
- 移除 `from src.notification_sender.gotify_sender` 和 `ntfy_sender` 的 import
- 替换为 `from src.notification_utils import ...`
- `_dispatch_notification_test` 方法改为返回 "功能已移除" 的桩实现

### 11. 清理 `src/services/notification_diagnostics.py`
- 移除 `from src.notification_sender.*` import
- 替换为 `from src.notification_utils import ...`

### 12. 创建 `src/notification_utils.py`
- 从已删除的 sender 模块中提取 `resolve_ntfy_endpoint` 和 `resolve_gotify_message_endpoint` 两个工具函数

### 13. 清理 `scripts/e2e_test_feishu_app.py`
- 移除 `FeishuSender` 导入和调用，改为记录 "功能已移除" 日志

### 14. 删除旧的测试文件
删除 10 个测试文件：
- `tests/test_bot_market_command.py`
- `tests/test_bot_dispatcher_async.py`
- `tests/test_bot_status_command.py`
- `tests/test_discord_platform.py`
- `tests/test_feishu_stream.py`
- `tests/test_feishu_stream_ordering.py`
- `tests/test_ask_command.py`
- `tests/test_skill_load_warning.py`
- `tests/test_notification.py`
- `tests/test_notification_sender.py`

### 15. 清理 `tests/test_multi_agent.py`
- 移除 `TestResearchCommandTimeout` 类（依赖 `bot.commands.research`）

### 16. 清理 `tests/test_system_config_service.py`
- 移除了全部 15 个 `test_notification_channel_*` 测试方法和 `_notification_test_env` helper
- 替换为单个 `test_test_notification_channel_returns_disabled` 测试

## 未改动
- ✅ `LICENSE` — 未动
- ✅ `README.md` — 未动
- ✅ `src/analyzer.py` — 未动
- ✅ `src/core/pipeline.py` — 仅移除 BotMessage import，核心分析逻辑未动
